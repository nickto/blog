## Development

1. Clone the repo.
2. Clone all the submodules: `git submodule update --init --recursive`.
3. Add `use nix` to `.envrc`; run `direnv allow`.
4. Start the server: `hugo server --buildDrafts`.
